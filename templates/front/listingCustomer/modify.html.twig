{% extends 'base.html.twig' %}

{% block title %}Ajouter un client - {% block title_base %}{{ parent() }}{% endblock %}{% endblock %}

{% block body %}
	{% block content %}
		{% if notification %}
			<div class="form-success text-center text-bold">
				{{ notification }}<br>
				<a href="{{ path('listing_projects') }}">Retourner sur la liste clients</a>
			</div>
		<div class="separator"></div>
		<div class="mb-3"></div>
		{% endif %}
		<div class="form-general">
			<h1 class="text-center color-white">Modification du site <i class="fas fa-arrow-right"></i> {{listingCustomer.domainname}}</h1>	
		<div class="mb-3"></div>
		<div class="separator"></div>
		<div class="mb-5"></div>
			{{ form_start(form_listing_project_modify) }}
				<div>
				{{ form_row(form_listing_project_modify.domainname) }}
				<div class="mb-3"></div>
				<p class="text-left color-yellow">Activité(s)</p>
				{{ form_row(form_listing_project_modify.name) }}
				<div class="mb-3"></div>
				{{ form_row(form_listing_project_modify.websitetype) }}
				<div class="mb-3"></div>
				{{ form_row(form_listing_project_modify.submit) }}
				</div>
			{{ form_end(form_listing_project_modify) }}
		</div>
	{% endblock %}
	</div>
{% endblock %}